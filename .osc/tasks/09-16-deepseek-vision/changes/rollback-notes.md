# Rollback

Revert this task's helper and test changes, or redeploy the previous immutable
image if this change is deployed later. No config, data, or schema migration is
needed. Restoring the old guard also restores the V4.1 Flash image rejection.

Production was not modified during implementation.
