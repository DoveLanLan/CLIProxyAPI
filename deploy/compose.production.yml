services:
  cli-proxy-api:
    image: ${CLI_PROXY_IMAGE:?set CLI_PROXY_IMAGE}
    container_name: cli-proxy-api
    restart: unless-stopped
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      GROK_INSPECTION_DATA_DIR: /var/lib/cliproxyapi/grok-inspection
    ports:
      - "${TAILSCALE_BIND_IP:?set TAILSCALE_BIND_IP}:${TAILSCALE_MANAGEMENT_PORT:-18317}:8317"
    expose:
      - "8317"
    volumes:
      - ./data/config.yaml:/CLIProxyAPI/config.yaml
      - ./data/auths:/root/.cli-proxy-api
      - ./data/logs:/CLIProxyAPI/logs
      - ./data/plugins:/CLIProxyAPI/plugins
      - ./data/grok-inspection:/var/lib/cliproxyapi/grok-inspection
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: ${GATEWAY_NETWORK:-vps-gateway}
